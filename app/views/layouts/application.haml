!!!
%html{html_attrs(Locale.get)}
%head
  %meta{ :content => "text/html; charset=utf-8", "http-equiv" => "Content-Type" }
  - headers["Content-Type"] = "text/html; charset=utf-8"
  = stylesheet_link_tag('labvirt.css')
  %title
    = @title
  = javascript_include_tag :defaults
%body
  #header
    = @title
    #locales
      = locale_links
  #menu
    = @menu
  #main
    = login_data
    = show_flash
    #contents
      = yield
  #footer
    = @footer
